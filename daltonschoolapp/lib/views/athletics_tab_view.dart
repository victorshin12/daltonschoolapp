import 'package:flutter/material.dart';

class AthleticsTabView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Athletics Tab"),
      )
    );
  }
}